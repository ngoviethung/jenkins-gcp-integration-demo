apiVersion: apps/v1
kind: Deployment
metadata:
  name: consumer-vote
  labels:
    tier: backend
    layer: queue-consumer-vote
    queue: default
spec:
  replicas: 2
  selector:
    matchLabels:
      tier: backend
      layer: queue-consumer-vote
      queue: default
  template:
    metadata:
      labels:
        tier: backend
        layer: queue-consumer-vote
        queue: default
    spec:
      containers:
        - name: queue-consumer-vote
          image: hungnv93/sdu01-cli:v0.0.2
          command:
            - php
          args:
            - artisan
            - rabbitmq:consume
            - vote
          ports:
            - containerPort: 9000
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secret
          resources:
            limits:
              cpu: "0.5"
              memory: "512Mi"
            requests:
              cpu: "200m" #tuong duong 0.5 cpu
              memory: "256Mi"
